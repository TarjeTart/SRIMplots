Requirments:
- Requires installation of SRIM
- This application runs on Java 1.8
- When prompted to select SRIM location make sure to point to SRIM folder and not inside it

Outputs:
- The program will open a plot of the stopping ranges with the energies and units specified by the user
- A png of the plot and a text file with the raw data are also saved in the SR Module folder within the SRIM directory
